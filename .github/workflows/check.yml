name: Check
on: [push]
jobs:
    check:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: 🪴 Install dependencies
              run: bun install
            - name: 😎 Check
              run: bun biome ci